import type { Metadata } from 'next';

export const metadata:Metadata = {
  title:'Privacy Policy | FleetFlow',
  description:'How FleetFlow handles personal data for guests, renters, and rental companies in Saudi Arabia, Yemen, and supported markets.',
};

export default function PrivacyLayout({ children }:{ children:React.ReactNode }) { return children; }

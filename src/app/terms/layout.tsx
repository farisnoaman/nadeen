import type { Metadata } from 'next';

export const metadata:Metadata = {
  title:'Terms & Market Conditions | FleetFlow',
  description:'FleetFlow platform terms with distinct car-rental market conditions for Saudi Arabia and Yemen.',
};

export default function TermsLayout({ children }:{ children:React.ReactNode }) { return children; }

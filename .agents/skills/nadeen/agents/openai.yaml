interface:
  display_name: "Nadeen"
  short_description: "Repo-specific patterns and workflows for nadeen"
  default_prompt: "Use the nadeen repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true